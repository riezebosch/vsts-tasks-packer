{
  "name": "vsts-packer-tasks",
  "version": "0.0.1",
  "description": "Packer Tasks for VSTS",
  "main": "src/index.js",
  "scripts": {
    "build": "tsc -p .",
    "build:prod": "npm i && npm run build && npm run test && npm run stryker -- --reporters clear-text,progress && npm prune --production && npm dedupe --production",
    "pretest": "npm run build",
    "test": "mocha --recursive",
    "stryker": "stryker run"
  },
  "keywords": [
    "packer",
    "vsts"
  ],
  "author": "Manuel Riezebosch",
  "license": "ISC",
  "dependencies": {
    "azure-pipelines-task-lib": "^3.3.1",
    "fs": "0.0.1-security"
  },
  "devDependencies": {
    "@stryker-mutator/core": "^6.0.2",
    "@stryker-mutator/mocha-runner": "^6.0.2",
    "@types/chai": "^4.3.1",
    "@types/mocha": "^9.1.1",
    "@types/node": "^17.0.36",
    "@types/sinon": "^10.0.11",
    "@types/uuid": "^8.3.4",
    "chai": "^4.3.6",
    "mocha": "^10.0.0",
    "sinon": "^14.0.0",
    "typescript": "^4.7.2"
  }
}
